export class TableDisplay {
    first: string;
    last: string;
    email: string;
    birthday: string;

    constructor(first: string, last: string, email: string, birthday: string) {
        this.first = first;
        this.last = last;
        this.email = email;
        this.birthday = birthday;
    }
}
